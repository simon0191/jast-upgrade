apply plugin: 'application'

dependencies {
  compile project(':processors')
}

compileJava {
  options.compilerArgs = [
    "-processor",
    "pw.smn.jastupgrade.processors.StringFromCommentProcessor"
  ]
}

mainClassName = "pw.smn.jastupgrade.examples.StringFromCommentExample"
